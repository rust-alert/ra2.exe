# config.toml.example

# 游戏安装目录（含 MIX / INI）
ra2_dir = "C:/path/to/your/ra2"

# 可选：强制版本。不写则自动探测。
# edition = "ra2"
# edition = "yr"
# edition = "mo3"

# 战网 Alpha（可选）。未接线时仅记录日志，不打开 socket。
# net_url = "ws://127.0.0.1:7788"
# net_room = "alpha-skirmish"
