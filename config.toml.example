# config.toml.example

# 游戏安装目录（含 MIX / INI）
ra2_dir = "C:/path/to/your/ra2"

# 可选：强制基础版本探测提示。不写则自动探测。
# edition = "ra2"
# edition = "yr"
# edition = "mo3"

# 联机接入预留（协议与目标战网未定点前不接 socket）
# net_url = "…"
# net_room = "…"
