# RustAlert 桌面启动配置
#
# 将本文件复制为可执行文件同目录下的 `RustAlert.toml` 后按需修改。
# 分辨率、显示与其它启动选项写在本文件中。
# 默认情形：未写 `ra2_dir` 时取 exe 所在目录——把 `rust-ra2` / `rust-ra2.exe`
# 放进游戏安装目录即可定位资源，无需再写安装路径。这只是 `ra2_dir` 的默认，不是「无需本文件」。

# ra2_dir = "C:/path/to/your/ra2"
# edition = "ra2"   # 或 "yr"；省略则按目录特征自动探测
# net_url = ""      # 预留战网地址
# net_room = ""     # 预留房间名
